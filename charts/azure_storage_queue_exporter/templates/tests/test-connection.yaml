apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "azure_storage_queue_exporter.fullname" . }}-test-connection"
  labels:
    {{- include "azure_storage_queue_exporter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "azure_storage_queue_exporter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
