apiVersion: v1
kind: Service
metadata:
  name: {{ include "azure_storage_queue_exporter.fullname" . }}
  labels:
    {{- include "azure_storage_queue_exporter.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "azure_storage_queue_exporter.selectorLabels" . | nindent 4 }}
